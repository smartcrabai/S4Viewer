# S4Viewer Agent Rules

- TDD is mandatory. Add a failing test -> minimal implementation -> passing tests -> refactor.
- Non-screen-occupying E2E is mandatory. Run `./Scripts/run-intent-tests.sh` and verify that all `IntentAutomationTests` pass.
